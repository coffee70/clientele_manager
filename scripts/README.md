# Scripts

Python utility scripts for the Clientele Manager project.

## Requirements

```bash
pip install -r ../requirements.txt
```

## Scripts

| Script | Description |
|--------|--------------|
| `generate_app_icon.py` | Generates a 1024×1024 PNG app icon (black background, white ring) and writes it to `frontend/Clientele Manager/Clientele Manager/Assets.xcassets/AppIcon.appiconset/AppIcon.png` |

## Usage

```bash
python generate_app_icon.py
```
